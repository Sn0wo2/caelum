[package]
name = "sage-trace"
version = "0.0.0"
edition = "2024"
description = "A customizable logging library for Rust"
repository = "https://github.com/Sn0wo2/sage-trace"
license = "MIT"
keywords = ["tracing", "logging", "formatting", "colors", "theme"]
categories = ["development-tools::debugging", "development-tools::profiling"]
exclude = ["src/debug/", ".github/", ".idea/", "img.png"]

[features]
default = ["unicode", "file"]
async = ["custom-async", "native-async"]
custom-async = ["dep:tokio"]
native-async = ["file"]
file = ["dep:tracing-appender"]
compress = ["dep:flate2"]
serde = ["dep:serde"]
unicode = []
nerd = ["dep:nerd-font-symbols"]

[dependencies]
derive_more = { version = "1", features = ["display", "from"] }
smart-default = "0.7"
thiserror = "2"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
flate2 = { version = "1.1.9", default-features = false, features = ["miniz_oxide"], optional = true }
owo-colors = "4.3.0"
serde = { version = "1", features = ["derive"], optional = true }
tracing = "0.1"
tracing-appender = { version = "0.2", optional = true }
nerd-font-symbols = { version = "0.3.0", optional = true }
tokio = { version = "1", default-features = false, features = ["io-std", "io-util", "rt", "sync"], optional = true }
tracing-subscriber = {
    version = "0.3",
    features = ["env-filter", "fmt", "json", "registry", "ansi", "std"]
}

[build-dependencies]
sage-trace-build = { version = "0.0.0", path = "crates/sage-trace-build" }

[[bin]]
name = "debug"
path = "src/debug/debug.rs"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
incremental = false
